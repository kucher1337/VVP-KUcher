#include <stdio.h> //Подключение библиотеки ввода/вывода

#include <stdlib.h> //Подключение библиотеки


int main()
{
	double a = 3, b = 5, S;
	S = (a + b) / 2;
	printf("%f\n", S);
	system("pause");
	return 0;
}